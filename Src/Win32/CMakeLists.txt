AUX_SOURCE_DIRECTORY(. SRC)

FILE(GLOB_RECURSE INC "*.h")
FILE(GLOB_RECURSE INC_TEMP "*.inl")
LIST(APPEND INC ${INC_TEMP})

INCLUDE_DIRECTORIES(
../Engine;
)

ADD_STATIC( win32App "${INC}" "${SRC}")
ADD_PRECOMPILED_HEADER( Win32App "sgfWin32AppPCH.h" "./sgfWin32AppPCH.cpp" SRC)